# Copy this to deals-bot/.env.local on the M2 MacBook and fill in real values.
# deals-bot/.env.local is gitignored — never commit it.
#
# Get these from Supabase project settings (Project Settings > API), or from
# the castzone-website repo's GitHub Actions secrets if you have them handy.
SUPABASE_URL=https://zohgnpogjtjnozyweutg.supabase.co
SUPABASE_SERVICE_KEY=

# Deliberately NOT set here: SCRAPER_API_KEY. The whole point of self-hosting
# on this Mac is that its home IP isn't datacenter-blocked, so no proxy is
# needed — leave this unset.
